# ── 本地审查/审核报告（工作产物，不进分发包）──────────────────
# 用通配符，覆盖后续各轮审查报告（R1/R2/R3…）
REVIEW-*.md
AUDIT-*.md

# ── 技能自我升级的提案产物（待人工批准后由外部安装器应用）──────
.skill-upgrade/

# ── Python 编译产物 ─────────────────────────────────
__pycache__/
*.pyc
*.pyo

# ── 运行时产物 ──────────────────────────────────────
.logs/
.update-notification.md
*.bak

# ── 安装器本机元数据 ────────────────────────────────
.clawhub/

# 注意：.scaling-state.json 属于包元数据（记录档位演进史），应当入库，不在此忽略。
